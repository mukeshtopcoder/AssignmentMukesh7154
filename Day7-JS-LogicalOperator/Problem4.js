/*determine if a given character is a vowel or a consonant. If the character is a lowercase or uppercase vowel (a, e, i, o, u, A, E, I, O, U), consider it a vowel; otherwise, consider it a consonant.*/
var ch = 'u';
if(ch=='a' || ch=='e' || ch=='i' || ch=='o' || ch=='u' || ch=='A' || ch=='E' || ch=='I' ||ch=='O' || ch=='U')
    console.log("Vowel");
else
    console.log("Consonants");