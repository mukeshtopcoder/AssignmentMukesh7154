/* program for inventory management. If an item is out of stock (isOutOfStock is true), display a notification. Use the logical NOT operator to check if the item is in stock*/
var isOutOfStock = true;
if(!isOutOfStock)
    console.log("Yes, Stock is Short!");
else
    console.log("Stock Available!");