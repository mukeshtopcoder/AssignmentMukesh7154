/*Build a weather app that informs users about the weather conditions. If the weather forecast predicts no rain (!isRaining), display a message indicating that it's a good day for outdoor activities*/
var isRaining = true;
if(!isRaining)
    console.log("Rainy Day!");
else
    console.log("It's A Good Day!");